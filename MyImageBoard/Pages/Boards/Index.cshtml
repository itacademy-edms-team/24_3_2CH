@page
@model ForumProject.Pages.Boards.IndexModel
@{
    ViewData["Title"] = "Доски форума";
}
<link rel="stylesheet" href="~/css/boards-index.css" />

<div class="main-header" style="display: flex; align-items: flex-start; justify-content: center; gap: 2.5em; margin-bottom: 1.5em; margin-top: 1.2em;">
    <div class="main-header-title" style="font-size: 4em; font-weight: 900; color: #b88a00; letter-spacing: 0.04em; line-height: 1.05; margin-bottom: 0.2em; margin-top: 0.2em;">Mimesis</div>
    <div class="main-header-image" style="width: 100px; height: 100px; background: #f5f5dc; border: 2px dashed #e0c97f; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.2em; color: #e0c97f; margin-top: 0.2em;">
        <img src="/img/mask.png" alt="Mimesis mask logo" width="80" height="80" style="display:block; margin:auto;" />
    </div>
</div>
<div class="main-description" style="font-size: 1.25em; color: #444; margin-bottom: 2.5em; max-width: 1000px; margin-left: auto; margin-right: auto; text-align: left;">
    Добро пожаловать на Mimesis — уютный форум для общения, обмена мемами, обсуждения фантастики, технологий и всего, что душе угодно. Здесь каждый может создать тред, поделиться мыслями или просто понаблюдать за жизнью борды.
</div>

@if (Model.Boards != null && Model.Boards.Any())
{
    <table class="boards-table">
        <thead>
            <tr>
                <th>Название</th>
                <th>Описание</th>
                <th>Создана</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var board in Model.Boards)
            {
                <tr>
                    <td>
                        <a asp-page="Details" asp-route-id="@board.Id">@board.Title</a>
                    </td>
                    <td>
                        @board.Description
                    </td>
                    <td>
                        @board.CreatedAt.ToShortDateString()
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Досок пока нет.</p>
}